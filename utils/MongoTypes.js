export const mongoReqStr = {
  type: String,
  required: true,
};

export const mongoReqNum = {
  type: Number,
  required: true,
};

export const mongoReqDate = {
  type: Date,
  required: true,
};

export const mongoDefNum = {
  type: Number,
  default: 0,
};
